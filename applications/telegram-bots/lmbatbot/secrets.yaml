apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lmbatbot-env
spec:
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  data:
    - secretKey: TELEGRAM_TOKEN
      remoteRef:
        key: telegram-bots/lmbatbot/telegram-token
