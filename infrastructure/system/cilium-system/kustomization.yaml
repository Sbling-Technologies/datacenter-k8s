apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - repo: https://helm.cilium.io/
    name: cilium
    releaseName: cilium
    version: 1.18.1
    includeCRDs: true
    valuesFile: values.yaml
    namespace: kube-system

resources:
  - base/l2-announcements.yaml
  - monitoring/dashboards.yaml
